# 媒体查询

- 媒体查询是一种提供给开发者针对不同设备需求进行定制化开发的一个接口

- 可以根据设备的类型(**比如屏幕设备 ,  打印机设备 , 或者特定的特定**)来修改自己的页面

- 媒体查询的三种使用方法

  1. 通过@media和@import使用不同的CSS规则

     ```html
     <style>
       @import url(css文件) (媒体查询规则)
     </style>
     ```

  2. 使用media属性为<style> , <link> , <source>和其他HTML元素指定特定的媒体类型

  3. 使用Window.matchMedia()  和 MediaQueryList.addListtener()方法来测试和监控媒体状态



# 媒体查询 - 媒体类型

- 使用媒体查询时，你必须的指定要使用的媒体类型
  - 默认是all类型
- 常见类型
  - all：适用于所有设备
  - print：使用于打印预览模式下在屏幕上查看的分页资料和文档
  - screen：主要用于屏幕
  - speech：主要用于语言合成器



# 媒体查询 - 媒体特性

- 媒体特性描述了浏览器。输出设备 或者预览环境的具体特征

  - 通常会将媒体特性描述为一个表达式

  - 每条媒体特型表达式都必须用括号括起来

    ![medie-character](D:\OneDrive\桌面\medie-character.png)

## 媒体查询 - 逻辑操作符

- 媒体查询的表达式最终会获得一个Boolean值，为true或者为false
  - 为true 生效
  - 为false 不生效
- 有多个条件，通过逻辑操作符
  - and ：and 操作符用于将多个媒体查询规则组合成单条媒体查询
  - not ：not运算符用于否定媒体查询，如果不满足这个条件则返回true，否则返回false
  - only ：only运算符仅在整个查询匹配时才用于应用样式
  - ，：逗号用于将多个媒体查询合并为一个规则